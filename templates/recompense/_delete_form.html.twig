<form method="post" action="{{ path('app_recompense_delete', {'id': recompense.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ recompense.id) }}">
    <button class="btn">Delete</button>
</form>
