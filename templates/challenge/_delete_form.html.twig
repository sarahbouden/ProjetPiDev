<form method="post" action="{{ path('app_challenge_delete', {'id': challenge.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ challenge.id) }}">
    <button class="btn">Delete</button>
</form>
