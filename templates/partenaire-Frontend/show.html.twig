{% extends 'base-back.html.twig' %}

{% block title %}Partenaire{% endblock %}

{% block body %}
    <h1>Partenaire</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ partenaire.id }}</td>
            </tr>
            <tr>
                <th>NomP</th>
                <td>{{ partenaire.NomP }}</td>
            </tr>
            <tr>
                <th>TypeP</th>
                <td>{{ partenaire.TypeP }}</td>
            </tr>
            <tr>
                <th>DescriptionP</th>
                <td>{{ partenaire.DescriptionP }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_partenaire_index') }}">back to list</a>

    <a href="{{ path('app_partenaire_edit', {'id': partenaire.id}) }}">edit</a>

    {{ include('partenaire_Backend/_delete_form.html.twig') }}
{% endblock %}
